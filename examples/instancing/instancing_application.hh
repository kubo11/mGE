#ifndef MGE_INSTANCING_APPLICATION
#define MGE_INSTANCING_APPLICATION

#include "mge.hh"

class InstancingApplication : public mge::Application {
 public:
  InstancingApplication();
};

#endif  // MGE_INSTANCING_APPLICATION